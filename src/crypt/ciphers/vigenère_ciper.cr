module Crypt::Ciphers
  class VigenèreCipher < TabulaRecta
    # I must see this to see how to use the tabula to create a vigenère
    # http://crypto.interactive-maths.com/other-examples.html

    #def encrypt(string, key, *args, **opts)
    #  super(Ciphers.atbash(@alphabet).encrypt(string.to_s), key.to_s, *args, **opts)
    #end
  end
end